ALL=`ls | sort -R`
for NAME in $ALL
do
	if [ -d $NAME ]
	then
		echo $NAME
		if [ $(( ( RANDOM % 3 ) )) -gt 0  ]
		then
			$NAME/$NAME &
		else
			$NAME/$NAME-large &
		fi
	fi

done

pgrep -P $$


#sleep 10

#kill `pgrep -P $$`
